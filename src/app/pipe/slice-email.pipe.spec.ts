import { SliceEmailPipe } from './slice-email.pipe';

describe('SliceEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
