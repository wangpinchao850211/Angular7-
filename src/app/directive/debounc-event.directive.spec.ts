import { DebouncEventDirective } from './debounc-event.directive';

describe('DebouncEventDirective', () => {
  it('should create an instance', () => {
    const directive = new DebouncEventDirective();
    expect(directive).toBeTruthy();
  });
});
