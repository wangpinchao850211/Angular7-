import { EmailvalidDirective } from './emailvalid.directive';

describe('EmailvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailvalidDirective();
    expect(directive).toBeTruthy();
  });
});
